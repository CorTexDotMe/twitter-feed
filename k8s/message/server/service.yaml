apiVersion: v1
kind: Service
metadata:
  name: message-service
spec:
  type: NodePort
  selector:
    app: message-server
  ports:
    - protocol: TCP
      port: 8888
      targetPort: 8888 # container port